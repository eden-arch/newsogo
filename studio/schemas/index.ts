import { page } from './documents/page'
import { heroSection } from './objects/heroSection'
import { heroSliderSection } from './objects/heroSliderSection'
import { logoGridSection } from './objects/logoGridSection'
import { textSection } from './objects/textSection'
import { siteHeader } from './documents/siteHeader'
import { siteFooter } from './documents/siteFooter'

export const schemaTypes = [page, heroSection, heroSliderSection, logoGridSection, textSection, siteHeader, siteFooter]
